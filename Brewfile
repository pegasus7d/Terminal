# GUI apps, fonts, and formulae that Nix handles poorly on macOS.
# Apply with:  brew bundle --file=~/dotfiles/Brewfile

cask "ghostty"                          # terminal emulator
cask "font-jetbrains-mono-nerd-font"    # prompt/editor font (icons + ligatures)

brew "scooter"                          # search & replace TUI
